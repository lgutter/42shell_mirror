image: lgutter/codam_ci:latest

variables:
  GIT_SUBMODULE_STRATEGY: recursive

stages:
  - test

simple-test:
  stage: test
  before_script:
    - meson build -Dbcoverage=true
    - ninja -C build
  script:
    - build/test-cetushell
  after_script:
    - ninja -C build coverage --verbose
  artifacts:
    paths:
      - build/meson-logs/coveragereport/
